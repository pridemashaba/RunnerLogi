// Supabase connection tests removed.
// This repo currently uses localStorage + cookies demo auth in `lib/auth.ts`.

function testConnection() {
  console.log('Supabase tests removed (not configured in this build).');
}

testConnection();

